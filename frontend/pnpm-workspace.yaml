packages:
  - "apps/*"
  - "packages/*"

# Разрешаем сборочные скрипты явно: pnpm 10 по умолчанию их блокирует,
# и без этого списка установка в CI и в образе выводит предупреждение.
onlyBuiltDependencies:
  - esbuild
