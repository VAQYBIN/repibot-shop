"""Репозитории: запросы к таблицам без бизнес-правил.

Ни один метод не вызывает `commit` — транзакцией владеет сервис. Иначе
несколько изменений одного сценария перестают быть атомарными: письмо в
`outbox` окажется зафиксированным раньше, чем пользователь, для которого оно
написано.
"""
